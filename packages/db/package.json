{
  "name": "database",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "npm run with-env prisma db push --skip-generate",
    "db:dev": "npm run with-env prisma studio --port 5556",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "dotenv-cli": "^7.0.0",
    "prisma": "latest"
  }
}
